Run backend container locally:
docker run --name backend --rm -p 8000:8000 backend

Run and shut down services locally via docker compose:
docker compose up
docker compose down

Run and rebuild services locally via docker compose:
docker compose up --build

Kill postgres if already running:
sudo lsof -i :5432
sudo kill **PID**

Start up dev server locally:
fastapi dev main.py

Login to postgres container:
docker exec -it **container id** psql -U postgres app